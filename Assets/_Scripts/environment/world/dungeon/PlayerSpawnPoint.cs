using roguelike.system.singleton;

namespace roguelike.environment.world.dungeon {
    public class PlayerSpawnPoint : Singleton<PlayerSpawnPoint> {
        // another empty object, this time it acts as an anchor for spawning the player
    }
}